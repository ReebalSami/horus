# Architecture Decision Records

| ID | Title | Status |
|---|---|---|
| ADR-001 | Tool-decision discipline (mandatory ADR sections for every tool/model/library/dataset/framework choice) | Accepted |
| ADR-002 | Source-archival convention (`docs/sources/` tree + Obsidian-clipper-compatible stubs) | Accepted |
| ADR-003 | Brand naming — HORUS (Hybrid OCR-free Reading & Understanding System) | Accepted |
| ADR-004 | Config library — Pydantic Settings + PyYAML (implements `horus-config-discipline` Bundle 1) | Accepted |
| ADR-005 | Synthetic ZUGFeRD invoice generator — dual-track: `factur-x` (Python) for generation + Mustang Project (Java) for cross-tool validation | Accepted |
| ADR-006 | Visual PDF renderer for synthetic ZUGFeRD invoices — fpdf2 (py-pdf ecosystem) | Accepted |
| ADR-007 | Local-VLM inference framework — dual-track: MLX-VLM (primary, MLX-ported §8.1 cohort) + HuggingFace Transformers + MPS (fallback, non-MLX-ported cohort) | Accepted |
| ADR-008 | Orchestrated-baseline document pipeline — dual-track: Docling (primary, IBM Research) + MinerU pipeline backend (cross-check, OpenDataLab) | Accepted |
| ADR-009 | Pilot-loop VLM cohort selection — 10 models in 3 categories (end-to-end / architecturally innovative / general multimodal); resolves the "cohort ADR #14" forward-pointer from ADR-007 and ADR-008 | Accepted (amended 2026-05-15 — Amendment 1: evidence-limitation reframing + XML-grounded factual corrections) |
| ADR-010 | ZUGFeRD XML-extraction script — ratifies `factur-x` (already-installed Python library) as canonical extraction engine + Mustang as opt-in cross-tool verification route; ships `scripts/extract_zugferd_xml.py` as load-bearing infrastructure for pilot #13's XML-grounded F1 evaluation harness | Accepted |
| ADR-011 | Experiment-tracker integration — MLflow (full package, self-hosted local SQLite + filesystem artifact root); extends `Tracker` Protocol with `start_run`/`end_run`/`log_dict`/`set_tag` for pilot #13's per-field heatmap + hardware-fingerprint logging; closes `horus-config-discipline` Bundle 4 via additive L3 promotion to the `python-ml-uv` template | Accepted |
| ADR-012 | CII XML → ground-truth field dict — parser substrate for pilot #13's per-field F1 evaluation; ships `src/horus/eval/ground_truth.py` with 16-field EN16931-anchored English-keyed dict (`GroundTruth(header=...)`, BT-* preserved as record attribute), three-route dict-equivalence proven across the FX corpus, normalizer dispatch via `FieldSpec` registry; pilot #13's PR(a) of the locked 3-PR split | Accepted |
| ADR-013 | VLM prediction scorer — VLM raw output → predicted dict → per-field F1 against `GroundTruth`; ships `src/horus/eval/{scorer,adapters,anls}.py` triple, additive `field_type` amendment to `FieldSpec`, `EvalConfig` Pydantic sub-model, two-layer adapter (per-model preprocessor + unified German-label extractor), per-field-type comparators (ANLS\* for STRING + exact-on-normalized for MONEY/DATE/CODE), 12-cell truth table, micro/macro F1 aggregation; pilot #13's PR(b) of the locked 3-PR split; documents page-1-only smoke baseline as honest limitation deferred to PR(c) (ADR-014) | Accepted |
| ADR-014 | Cohort harness + multi-page rasterizer (pypdfium2) — pilot #13's PR(c) of the locked 3-PR split; ships `src/horus/eval/{rasterize,harness}.py`, `pypdfium2`-backed multi-page rasterization replacing the page-1-only `sips` baseline, `(model × invoice) → MLflow nested-run` orchestrator with `mlflow.search_runs`-based resume-safety, factur-x XML extraction route for ALL ZUGFeRD fixtures (NOT sidecar route per ADR-012 Probe 5), per-field heatmap PNG via `mlflow.log_figure` on parent run, threshold-sensitivity ablation at τ ∈ {0.3, 0.5, 0.7}, first thesis-defense-quality F1 numbers on the full 26-invoice FeRD ZUGFeRD corpus × 7 working ADR-009 cohort models; lifts the page-1-only scope ADR-013 deferred here; closes issue #13 | Accepted |
| ADR-015 | MLflow UI Makefile target (`make mlflow-ui`) — wires `mlflow server --backend-store-uri sqlite:///mlflow.db --host 127.0.0.1 --port 8080` into the project Makefile; supersedes ADR-011 §"What this ADR does NOT decide" line 306 deferral; documents `mlflow ui ≡ mlflow server` empirical equivalence in MLflow 3.12.0; locks port 8080 default to avoid documented macOS AirPlay Receiver conflict at MLflow's port-5000 default; closes issue #45 | Accepted |
| ADR-016 | Fast dev config + adapter-iterate harness mode — multi-file YAML composition via pydantic-settings native `deep_merge` (extends `ExperimentConfig.from_yaml` to accept `str \| list[Path]`); side-by-side adapter A/B re-scoring against cached transcripts via `scripts/rescore.py` (renamed from `ablation_threshold.py`) with stability self-check (Google Rules of ML §24) + opt-in MLflow logging (canonical 2-runs pattern); `dev_only` schema field for HARKing-prevention tier separation; `make adapter-iterate` Makefile target for the ~5-15s fast dev loop; closes issue #51 | Accepted |
| ADR-017 | Timing + tokens/sec + GPU-memory instrumentation for the cohort harness — extends `ExtractionResult` with `generation_tokens`/`generation_tps`/`peak_memory_gb` fields populated from MLX-VLM `GenerationResult` (MLX backends) + post-generation token-id shape (Transformers-MPS); aggregates per-tuple `perf.*` MLflow metrics (Amendment 1: split into `decode_tps_mean` decode-only AA-canonical + `inference_tps_mean` end-to-end + `tags.perf.decode_tps_available` availability tag — corrects original formula's AA-methodology claim after web-fetch re-verification; chars/sec as tokenizer-agnostic sanity check; peak memory via MLX `mx.get_peak_memory()` OR Transformers-MPS pre/post `torch.mps.driver_allocated_memory()` snapshot per pytorch/pytorch#104188 limitation); parent-level MPS-recommended-max ceiling for `%_max` derivation; extends `scripts/inspect_pilot_13.py` with `_print_perf_table` (plain-text, no `rich` dep, two TPS columns per Amendment 1, sorted by mean wall-clock ascending, graceful degradation for pre-#52 parents + backward-compat read of legacy `perf.generation_tps_mean` as e2e_tps); `make inspect-pilot-13` Makefile target; **substrate** for the **H8 efficiency** hypothesis (latency-efficiency comparison; pre-registered 2026-05-31 via ADR-031 — corrects an earlier mislabel as "§6 H4", which is the Layer-3 graph-vs-vector hypothesis) — the H8 test itself filed separately; closes issue #52 | Accepted (Amendment 1 ratified 2026-05-20 — formula correction) |
| ADR-018 | Structured-output prompting probe — extends the canonical pilot-13 harness with two declarative `CohortConfig` fields (`prompt_template_override: dict[str, str] \| None` + `adapter_mode: Literal["regex", "json"]`) and a sibling `src/horus/eval/adapters_json.py` module (same `(preprocess, to_predicted_dict)` public surface as `adapters.py`); two YAML overlays (`pilot-13-structured-probe-uniform.yaml` + `pilot-13-structured-probe-native-json.yaml`) drive a 7-model × 1-invoice × 2-arm probe over `EN16931_Einfach`; pre-registered ≥3-of-7 schema-adherence threshold gates the conditional follow-on issue (#54 Experiment 2 fires) vs the fall-through path (#41 Layer 2 MONEY adapter + #55 LoRA fine-tuning ADR); overrides issue #53 body's "no harness changes / no ADR" tactical guards on scientific-correctness grounds (apples-to-apples comparison vs pilot-13 baseline + reviewer reproducibility via one `make` command + zero code debt because probe artifacts ARE the Experiment 2 substrate if probe succeeds); closes issue #53 | Accepted with amendments (post-audit DEFER verdict at `d01afd1` superseded by ADR-019 bug catalog + ADR-020 rescore methodology + ADR-021 verdict matrix amendments; new 2×2 verdict surface in `eval/probe-verdict-matrix.md`) |
| ADR-019 | Structured-output probe bug catalog — locks the empirical evidence catalog of every bug discovered during the ADR-018 probe post-verdict audit (Gemma-4 unfenced 2-dict JSON parse failure / GLM-OCR fence-bias asymmetry / Granite decoder-loop placeholder echo / pre-registered threshold schema-mimicry false-positive / multi-page JSON adapter test coverage gap / PaddleOCR-VL decoder-collapse / MinerU decoder-loop / PaliGemma2 base-VLM pre-registration inclusion error / `scripts/inspect_pilot_13.py` hardcoded-experiment-name limitation). Per-row mechanism cited from saved transcripts. Sign-off gate before any fix wave lands. | Accepted |
| ADR-020 | Probe rescore methodology — ratifies the offline-rescore-from-saved-transcripts policy + `rescore_of` MLflow tag convention for ADR-019 Phase 4 (and any future post-audit rescore). Treats saved transcripts as canonical evidence (HELM + EleutherAI lm-eval-harness `--log_samples` precedent); historical MLflow runs NEVER mutated; each rescore is a NEW parent run in the same experiment cross-linked via `rescore_of=<original_run_id>` tag. Documents the model-behavior-vs-extractor-bug classification rule: re-inference is required only when an extractor bug is demonstrated (Wave 3.3); model-behavior failures (Granite/PaddleOCR/MinerU decoder-loops on JSON OOD prompts) are diagnostic-only and do NOT require re-running. | Accepted |
| ADR-021 | Structured-output probe verdict matrix + threshold/denominator amendments — ratifies the 2 × 2 verdict surface (pre-registered ∧ amended F1≥0.1) × (N-of-7 PaliGemma counted ∧ N-of-6 PaliGemma flagged) introduced by `src/horus/eval/probe_verdict.py` (Wave 3.2). Amends ADR-018's single-verdict pre-registration with: (a) value-non-trivial gate `micro_F1≥0.1` to defend against schema-mimicry per ADR-019 §B4 (GLM-OCR Arm A passes pre-registered with 16 placeholder keys, F1=0); (b) N-of-6 denominator that flags PaliGemma2 as structurally non-participating per ADR-019 §B8 (HF model card: base VLM, knowable at probe-design time per ADR-009 §smoke). Honest disclosure: this is methodology-discovery (a new failure mode unanticipated in ADR-018 §"Predicted outcomes"), NOT goalpost-move (pre-registered cells preserved verbatim; amended cells reported alongside). Locks the canonical_keys gap (non-null-value count interpretation penalizes the most-honest extractor Gemma-4-E4B-it which emits null for genuinely-missing fields). | Accepted |
| ADR-022 | `scripts/` directory status — ratifies `scripts/` as a Python package (`__init__.py` already present since `8d1c44d`); resolves the silent contradiction between the existing `__init__.py` and three in-file comments stating *"scripts/ is not a package — load … via sys.path injection"* (`scripts/compute_probe_verdict.py:50`, `tests/test_rescore.py:41`, `tests/test_inspect_pilot_13.py:25` + module docstring line 14). Mechanical migration replaces `sys.path.insert(scripts/)` + bare `import rescore` with `from scripts import rescore` everywhere; standalone-invocable scripts (`python scripts/X.py` direct-path) that import siblings get a small `sys.path.insert(0, repo_root)` at module top, documented as a known Python invocation-discovery idiom; pytest gets `pythonpath = ["."]` in `[tool.pytest.ini_options]` so the repo root is on `sys.path` during test collection (replaces the now-removed per-file `sys.path` inserts). mypy resolves the imports natively; the 3 `import-not-found` errors of issue #62 collapse to zero. Unblocks the import-portion of #62. | Accepted |
| ADR-023 | GitHub Actions CI pipeline — first CI for HORUS; ratifies `.github/workflows/ci.yml` running `make lint` + `make typecheck` (cache-cleared per the `mypy` incremental-cache nondeterminism trap surfaced in the audit) + `make test` on push-to-main + every PR against main, on `ubuntu-latest`. Test-gating mechanism: `pytest.mark.skipif` decorators (`skip_if_no_corpus`, `skip_if_no_fixtures`, `requires_macos`) centralized in `tests/_corpus.py` — the canonical pytest-recommended pattern + scikit-learn precedent (`sklearn/utils/_testing.py`). Three-layer application: module-level `pytestmark` (5 test files), test-level decorators (15 tests in `test_harness.py`), parametrized-fixture auto-disappearance (corpus-empty parametrize → zero items collected). NO custom marker registration; NO `make test-ci` target; NO CLI-flag gymnastics — `make test` works identically in local and CI environments. Design evolution audit trail preserved in §"Design evolution" (Iteration 1 marker design → Iteration 2 first-CI-run amendments → Iteration 3 skipif-unification redesign per pytest official + user-driven research). Justifies the design over committing the corpus to git (size + license drift) and over CI runners with corpus mounted (cost + maintenance). Closes the audit-followups cluster's CI gap and removes the discipline-only enforcement of `make-sure-it-works` for landed PRs. Closes #64. | Accepted |
| ADR-024 | EDA visualization stack — adopts **Quarto** (Posit) as the rendering layer + **Plotly** (interactive) + **matplotlib**/**seaborn** (static editorial figures) for thesis-grade EDA artifacts. Solves the user-stated "HTML gets messy fast" concern (issue #46) by rendering jupytext `.py:percent` source-of-truth into ONE archived HTML book + PDF with cross-references, citations, page numbers, figure captions, and code-folding. Layered ON TOP of existing `notebook-discipline` (jupytext stays the source-of-truth; Quarto is an additional renderer alongside papermill, NOT a replacement). New deps: `plotly` + `seaborn` in `pyproject.toml` (matplotlib already transitive); Quarto CLI binary installed via `brew install quarto` on dev machine; CI does NOT render (rendering is dev-machine-only per ADR-023's lint+typecheck+test scope). New `make eda CFG=...` Makefile target chains jupytext-sync + Quarto execute + render. Hybrid aesthetic per Q5 of EDA plan: matplotlib/seaborn editorial-static figures (FT/NYT-influenced muted palette, baked-in annotations) for thesis-appendix; Plotly interactive figures (hover-to-see-PDF-name on heatmaps) in a separate "Interactive Explorer" section in HTML output (drops to static fallbacks in PDF). Substrate for issue #46 EDA on the 151-PDF ZUGFeRD corpus; pattern reusable for any future thesis-grade EDA artifact. | Accepted (extended by ADR-025: multi-dataset Book structure + Datasheets-for-Datasets template) |
| ADR-025 | EDA scope expansion — multi-dataset Quarto Book + Datasheets-for-Datasets template — extends ADR-024's single-dataset visualization stack into a thesis-grade Quarto Book covering all 7 datasets in `data/raw/` (ZUGFeRD German + fatura2 + funsd + inv-cdip-tobacco + parsee-ai English + cord-v2 Korean + omnidocbench multilingual). Adopts Gebru et al. 2018 *Datasheets for Datasets* (arxiv:1803.09010) as the per-chapter dataset-documentation template (motivation / composition / collection / preprocessing / uses / distribution / maintenance). Structural changes: one chapter per dataset (`experiments/0X-<slug>.py` jupytext source) + cross-corpus synthesis chapter (`experiments/08-cross-corpus.py`) + consolidated Datasheets appendix (`experiments/A1-datasheets.qmd`) + index/preface (`experiments/00-index.qmd`) + bibliography (`experiments/references.qmd`); `_quarto.yml` extended with `book:` section + chapter list; new `src/horus/eda/` Python package factors out shared helpers (corpus walking + figure styling + Datasheet renderer) from per-dataset loaders. Per-chapter `configs/eda-<slug>.yaml` follows `horus-config-discipline` (one Pydantic-validated YAML per chapter; HARKing safeguards pre-committed before EDA runs). Local-only rendering during iteration (`_book/index.html` opened via file://); GitHub Pages deployment deferred to a separate ADR + commit once the Book passes end-to-end review. Out-of-scope: scope decisions about which datasets to use for evaluation/training/fine-tuning (those happen in PRD/spec phase, not EDA), ZUGFeRDv1 parser extension (future ADR if/when needed), Belege chapter (private/PII; separate gitignored artifact later). Driven by user feedback that the original ZUGFeRD-only EDA scope missed the 6 other datasets sitting in `data/raw/`. | Accepted |
| ADR-026 | Cohort dashboard TUI — textual inline-app three-section live dashboard for `make cohort-smoke` and `run_cohort`: scrolling per-invoice top panel + pinned current-model middle bar + pinned sweep-wide bottom bar. Display-adapter pattern (`HorusDashboardAdapter` / `PlainDisplayAdapter` / `SilentDisplayAdapter`) keeps harness display-agnostic. `App.suspend()` around HF tqdm model loads. Pyfiglet HORUS banner at sweep start. Graceful plain-print fallback when no TTY. Amends ADR-017's "plain-text, no `rich` dep" deferral for `_print_perf_table` (dissolved by this PR declaring `rich` as an explicit dep). New `src/horus/cli/` package. | Accepted |
| ADR-027 | F1 evaluation-metric expansion — four additive metrics on top of the existing micro/macro F1 (`ADR-013` scorer), all derived offline from the per-field `outcome` data already logged (no VLM, no re-run): (1) **per-canonical-label F1** — pools TP/FP/FN per each of the 16 `FIELDS` keys across the cohort (surfaces which business terms are hard); (2) **presence-conditional F1** — F1 restricted to GT-present fields (recall-faithful, since HORUS's truth table structurally yields FP only on the absent-GT row); (3) **group-level F1** — KIEval (arXiv 2503.05488 §4.1) all-or-nothing group correctness over EN16931 business groups (seller / buyer / totals), excluding the document-level scalars as the non-group G′; (4) **spurious-emission rate** — FP/(FP+TN) hallucination rate on genuinely-absent fields. (2)+(4) decompose micro-F1 into its recall/precision axes and jointly resolve the `ADR-021` canonical_keys asymmetry that penalised Gemma-4 for honest nulls. Extends `src/horus/eval/scorer.py` (additive `InvoiceFieldScores` fields + pure metric functions + `FIELD_GROUPS` partition) + `scripts/inspect_pilot_13.py` (cohort-pooled print). Resolves DR-7; closes #74. | Accepted |
| ADR-028 | Section-scoped invoice-totals adapter fallback (Belegsummen) — recovers the 4 silently-missing MONEY fields (`line_total_amount` BT-106 / `tax_basis_total_amount` BT-109 / `tax_total_amount` BT-110 / `grand_total_amount` BT-112) that the regex adapter dropped because their EN16931 `german_label`s (`Summe Nettobeträge` / `Steuerlicher Bemessungsbetrag` / `Umsatzsteuer gesamt` / `Bruttobetrag`) never match the FeRD "Belegsummen" display labels models actually emit (`Positionssumme` / `Rechnungssumme ohne USt.` / `Steuerbetrag` / `Bruttosumme`). Adds a section-anchored, shape-tolerant Layer-2 fallback in `to_predicted_dict` (`src/horus/eval/adapters.py`) that locally normalizes the Belegsummen window across the 4 totals-emitting transcript shapes (Granite DocTags `<fcel>…<nl>` cells / MinerU flattened `label: value` / PaddleOCR label-then-value lines / Gemma markdown table); section-scoping resolves the "Steuerbetrag" VAT-table-header collision AND preserves the single-page guarantee (page-1 transcripts carry no Belegsummen). Additively extends `scripts/rescore.py` with the ADR-027 4-metric baseline-vs-candidate summary (reuses the public scorer helpers; no change to the A/B comparison itself — extends ADR-016). Supersedes the `0.4908` cohort-baseline note in ADR-014 §"Known limitation" / §"Empirical results". No-HARKing: the metric set (ADR-027) + this fix were pre-registered before any post-fix numbers existed. Closes #41 | Accepted |
| ADR-029 | Structured-output JSON baseline (HND-1) — the "clean JSON" extraction arm (`adapter_mode="json"`, ADR-018) re-run on a 6-invoice ZUGFeRD subset for the 3 cohort models the ADR-018 probe proved JSON-capable (Gemma-4 / olmOCR-2-7B / GLM-OCR); cites ADR-019/021 for the 4 structurally-incapable models (DocTags / decoder-collapse / base-VLM) rather than re-running them (hardware-prohibitive + scientifically redundant per `know-your-hardware` + ADR-020). Re-inference (not offline rescore) was required — JSON-mode transcripts existed for only 1 invoice. Reuses the pre-registered Arm B prompts (per-model native prefix + JSON suffix) verbatim. Reports the ADR-027 4-metric surface via the existing inspector (no report code written). **Key findings (HND-3 #76 inputs):** (1) honest-null vs hallucinate-on-absent is the headline JSON axis — Gemma-4 spurious-emission 0.000 (best F1 0.707, fastest) vs olmOCR 0.875 / GLM-OCR 0.500 at *identical* presence-conditional recall; (2) prompt-only JSON under-recovers the Belegsummen MONEY totals that ADR-028's regex fallback recovers (`line_total` 0.105 vs 0.681) — "switch to JSON" does not obviously beat "free-form + adapter"; (3) KIEval group-level F1 ≈ 0 (cohort 0.019). `dev_only=false` (reported in-sample/diagnostic baseline comparable to `pilot-13-full`); No-HARKing via pre-registered plan+config+metrics. New overlay `configs/json-baseline.yaml`; no production-code change. Closes #54 | Accepted |
| ADR-030 | Reading-ceiling + approach-comparison diagnostic (HND-3 gate reframe) — reframes #76 from "pick the JSON-vs-free-form winner now" to "diagnose both honestly + carry both forward (staged); final Layer-1 pick deferred to post-fine-tuning" (exploratory under §4.2 branches-on-results; exploratory→confirmatory continuum, arXiv 2503.08124 — corrects an earlier mislabel as "H2", which is the single-shot-vs-orchestrated hypothesis; see ADR-031). Ships `scripts/reading_ceiling.py` (offline, no VLM) + `src/horus/eval/transcripts.py` (shared transcript loader lifted from `rescore.py`, which is refactored to import it) + `make reading-ceiling` + the generated `eval/reading-ceiling-and-approach-comparison.md`. Adds (A) per-model raw-reading **ceiling** (upper-bound on extractable: a surface form of the GT value present in the raw transcript text); (B) the **parser-loss vs read-miss** split (readable-but-dropped = adapter problem; absent-from-raw = model problem), MONEY totals called out; (C) the **same-tuple 4-metric** free-form (post-ADR-028) vs native-JSON comparison on the 3 JSON-capable models × 6 shared invoices, per-model + pooled. Defuses the ADR-028 stale-MLflow landmine by re-scoring free-form LIVE from transcripts (never the F1=0.000 `pilot-13-full` saved scores). Determinism cross-check reproduces ADR-029 `json-baseline-metrics.txt`. **Diagnostic, NOT a verdict**; in-sample until the held-out Belege split (#78). Archives Tam et al. 2024 (arXiv 2408.02442, "Let Me Speak Freely?") + IBM Granite-Docling (single-model, anti-cascade) per `horus-source-archival`. Files follow-ups #88 (Pydantic JSON) / #89 (best-reader fine-tune) / #90 (Qwen2.5-VL) / #91 (parser-agent). Closes #76 | Accepted |
| ADR-031 | Hypothesis-label reconciliation to canonical brainstorm v2 §6 + pre-registration of **H8 (efficiency)** — verbatim §6 mapping (H1 local-vs-cloud / H2 single-shot-vs-orchestrated / H3 KG-fidelity / H4 graph-vs-vector / H5 query-routing / H6 conditional-agentic-retry); catalogs + corrects every mislabel across ADR-008/009/017/030 + scripts + README + INDEX (Class A architecture H1→H2; Class B efficiency H4→H8; Class C #76 H2→exploratory); corrects ADR-017's fabricated "brainstorm v2 §6 H4" efficiency citation (§6 had no efficiency hypothesis); formalizes H8 (efficiency, dated 2026-05-31, test pending → genuine pre-registration) adopting ADR-017's own ≥3×-MLX-vs-MPS + 16 GB-fit wording; H7 unchanged (floated transfer-learning candidate). External §6 file read-only → user syncs H8. | Accepted |
| ADR-032 | H8 efficiency sweep — per-model `decode_tps`/`inference_tps`/`peak_memory_gb`/`%_max` substrate for the pre-registered **H8** hypothesis (ADR-031), via a dedicated re-run of the ADR-014 cohort harness + ADR-017 (#52) instrumentation (the `pilot-13-full` run predates #52 → logged no perf). 8 models (7 ADR-009 working cohort + a deliberately-added **controlled-pair MPS twin** of granite-docling-258M, bf16 — the only quant-matched same-model MLX-vs-MPS comparison) × 1 invoice (`EN16931_Einfach`, n=1; efficiency metrics ~invoice-stable; caps `MinerU` the ~22-min swapper to one run); `dev_only: true` keeps perf out of the thesis F1 lineage. **Two-clause verdict** (per ADR-031 trigger (b)): (1) **memory-fit HOLDS for 7/8** — peak 1.31–8.40 GB (≤ 66 % of the 12.71 GB recommended MPS working set); `MinerU` alone breaches at 13.40 GB = 105.4 % (swap, reproducing ADR-028 independently); (2) **decode-≥3× NOT cleanly evaluable** — no MPS `decode_tps` exists (0.0 sentinel on all 3 MPS rows), the controlled pair is broken on the MPS side (granite-MPS emits 0 tokens → F1 0.000 vs MLX 0.800, so "comparable F1" fails — a transformers/idefics3 generation issue, follow-up #99), and the `e2e_tps` proxy is model⊥backend-confounded; honestly bounded as a documented final position, not a TODO. Surfaces the efficiency–accuracy frontier (granite-docling-mlx ~134× faster than MinerU at near-equal F1). Adds the `ibm-granite/granite-docling-258M` MPS entry to `COHORT_MANIFEST` (11th; excluded from ADR-009 invariants) + `configs/h8-efficiency.yaml`. Closes #77 | Accepted |
| ADR-033 | ZUGFeRD v1 (`CrossIndustryDocument`) CII ground-truth parser support — extends `parse_cii_xml` to auto-detect the CII schema by root element and parse BOTH v2 (`CrossIndustryInvoice`) and v1 (`urn:ferd:CrossIndustryDocument:invoice:1p0`); `FIELDS_V1` is DRY-derived from `FIELDS` via 7 mutually-disjoint container-element substitutions (the 16 EN16931 leaf paths proved byte-identical across v1/v2 — verified empirically against a real FeRD v1 reference invoice, disproving DR-1's "field-semantics differ" worry); adds `CII_NAMESPACES_V1` + `_select_schema` root detection (raises `ValueError` on non-CII roots — fail-fast); the ADR-014 harness auto-benefits (factur-x already extracts the v1 attachment), unlocking the 24 `ZUGFeRDv1/` corpus PDFs as GT-parseable for baselines + the fine-tuning pool; corrects now-stale "v1 = empty GT" docstrings in `horus.eda.zugferd_loader`; resolves Decision-Register DR-1. Closes #75 | Accepted |
| ADR-034 | Held-out evaluation strategy + Layer-1 reader/structurer down-select — reframes the Layer-1 approach from the in-sample 7-model cohort sweep to a focused, **language-agnostic** two-arm design on the frozen Belege held-out set (#78): **reader = Granite-Docling-258M (MLX)** (ties MinerU's 0.98 reading ceiling + 1.00 MONEY at ~134× the speed, fits the 16 GB envelope — ADR-030/032; MinerU demoted to slow accuracy-reference), **structurer = Gemma-4 (MLX)** for both arms (best+fastest+only-honest JSON model, spurious 0.000 — ADR-029). Arm A (single-shot image→Gemma→JSON) vs Arm B (orchestrated image→Granite→text→Gemma→JSON), same structurer isolating the value of specialist reading; the old German-regex adapter retained as a one-shot brittleness baseline + auditable no-hallucination reference. Arm-A-vs-B is **exploratory under §4.2** (NOT §6 H2 — H2 needs a specialist-pipeline arm, #50; per ADR-031). Locks the pre-registration (metrics / arms / dev-frozen split / honesty axis) before any frozen-set number exists; carries the full **GitHub board-reconciliation table** for the pivot (4 closed / 8 rescoped / 2 new). Master record cross-linked from ADR-035/036. | Accepted |
| ADR-035 | Canonical extraction schema extension — German-canonical **Pydantic `InvoiceFields`** schema (stable EN16931/§14-UStG keys; values as-printed in any language; reasoning-then-strict-JSON + post-hoc Pydantic validation/repair since MLX has no constrained decoding per ADR-018); adds **tax-rate** + **seller/buyer addresses** as objective scored scalars and a **generated purpose-summary** as a non-scored Streamlit demo field; full line-item recognition (LIR) deferred as a stretch. Extends/supersedes ADR-012's 16-field EN16931 contract (the schema doubles as the JSON-arm fine-tuning target — #88). | Accepted |
| ADR-036 | Streamlit observability application — modular, multipage research/eval dashboard at top-level **`app/`** (new top-level dir ratified per `clean-project-structure`); read-only data access from MLflow runs + saved transcripts + GT (no recomputation in the UI); first increment = **Invoice Explorer** (page image + transcript + extracted JSON + GT + colour-coded per-field score) + **Approach Comparison**; built on the current Streamlit multipage API (`st.navigation`/`st.Page`, verified via context7). Supersedes the ADR-026 `textual` TUI as the analysis surface (TUI retained for live cohort-run progress, not extended — closes #72/#85); the #82 FastAPI+Docker end-user prototype folds in later as a page (unified app). | Accepted |
| ADR-037 | Closed-milestone baseline freeze — ADR-035's 16→19 schema extension is **forward-only for scoring scope**: closed-milestone in-sample reproductions (ADR-014/028/029) stay 16-field so their PUBLISHED numbers never shift. Adds a backward-compatible `score(*, fields=...)` field-subset parameter + `ground_truth.LEGACY_EXPERIMENT_FIELDS` (the frozen 16-field set); threads `fields=` through `rescore.rescore_transcripts` + `reading_ceiling._process_dir`; the 3 reproduction tests pin to it. Resolves the internal tension in ADR-035 §Integration (*"regex baseline unaffected on its existing 16 fields"* vs *"metrics recompute over the larger field set"*) in favor of the former for historical reproductions. Rationale: scoring a 16-field-targeted system against 19 fields is a meaningless hybrid (it penalizes a system for fields it was never asked to extract); the milestone's honest measurement is 16-field. New work (structurer arms + held-out eval) always scores the full 19; zero published numbers change. Sub-decision of ADR-035. | Accepted |
| ADR-038 | Read-then-structure orchestration mechanism for the exploratory extraction arms — ratifies HOW Arm A (single-shot image→Gemma→fields) and Arm B (orchestrated image→Granite→text→Gemma→fields) are built on top of the ADR-034 strategy + ADR-035 schema, surfacing the gap ADR-034 §Integration glossed (it said the arms "slot in as `adapter_mode` siblings" but Arm B runs a *second model*, not a pure text→dict adapter). **Text-only structurer path:** Gemma consumes Granite's transcript text via the already-installed `mlx-vlm` (`generate(..., image=None)` + `apply_chat_template(..., num_images=0)` — the `gemma4` arch omits the image token at `num_images=0`); **no new dependency** (`mlx_lm` not required). Proven in `experiments/arm-b-structurer-probe.py` (EN16931_Einfach Granite transcript → Gemma text-only → 15/19 fields, ISO-date + canonical-money coercion via `validate_and_repair`, **spurious-free honest nulls**, 7.75 GB peak / ~35 s). **Wiring:** a new `src/horus/eval/structurer.py` (model JSON-ish text → `adapters_json` recovery ladder → `InvoiceFields.validate_and_repair` → 19-key dict) is the parser for Arm A through the harness; Arm B is a two-pass offline flow over cached Granite transcripts (reuse `transcripts.py` + the cohort harness for reading) + a new Gemma text-structuring pass — NOT shoehorned into the pure-function `adapter_mode` slot. Extends adapter dispatch past ADR-016/018's binary `{regex, json}` to a small named-approach set (acknowledges ADR-016 supersession trigger #3). Honesty axis (`spurious_emission`, ADR-027) measured on every approach as the tax-domain guardrail. **Scope:** the orchestration MECHANISM for the *exploratory* (§4.2) arms only; the confirmatory single-shot-vs-orchestrated-spine verdict + validator-loop stay deferred to the held-out set (#50 remains open for those). Sub-decision of ADR-034; partially activates #50; addresses #91's "an ADR is required" note. | Accepted |
| ADR-039 | Live single-method extraction demo page — a **bounded exception** to ADR-036 §D's read-only constraint: live VLM inference is permitted **only** on a new end-user page (`app/views/live_extraction.py`), while the three research/analysis surfaces (Overview / Invoice Explorer / Approach Comparison) **stay read-only**. The user uploads an invoice, picks ONE of **two AI methods** (Arm A single-shot Gemma; Arm B Granite→Gemma — the regex baseline is deferred as least relevant for an end-user audience), clicks **Read**, and the chosen pipeline runs live and renders the 19 fields + the non-scored `purpose_summary` (+ the Granite transcript for Arm B). **No scoring** (no ground truth for uploaded files) → human-eye review only, with an explicit "visual review only, not a scored result" banner (the tax-domain honesty guardrail). New pure-orchestration module `src/horus/eval/live.py` reuses the existing primitives (`rasterize_pdf` / `get_extractor` / `MLXVLMExtractor.extract`/`.extract_text` / `structurer.to_full_dict`) — **no MLflow, no scoring, NOT `run_arm_b`** — and is unit-tested with the extractor mocked. Model loaded once per session via `st.cache_resource` (NOT unloaded per click; ~9 GB resident for Arm B's two models — fits the 16 GB envelope per `know-your-hardware`). Rasterizes at the evaluation DPI (300, per `pilot-13.yaml`) so the demo faithfully reflects measured performance. All prompts/model-IDs read from `configs/arm-{a,b}.yaml` + `COHORT_MANIFEST` (per `horus-config-discipline`). Early **Layer-1 slice** of the #82 end-user prototype (folds in as a page per ADR-036 §C); does NOT close #82 (KG/Q&A/FastAPI/Docker out of scope). Sub-decision of ADR-036; cites #82/#103; closes #109. | Accepted |
| ADR-040 | Held-out Belege test set — ground-truth methodology + loader + privacy + circularity guard — builds the PRIVATE real-invoice held-out set (#78 / HND-5) the ADR-034 strategy requires. **GT shape = identical to the ZUGFeRD route**: the same 19-field `GroundTruth` the scorer already consumes (NOT a transcription; schema extension — IBAN/`Skonto`/`Zahlungsziele`/… — deferred to a new issue). Ships `src/horus/eval/heldout.py` (the **JSON→`GroundTruth` route**, parallel to `parse_cii_xml`, reusing `validate_and_repair` for locale repair + honesty/presence semantics; flat-layout discovery via a local `index.json`; `build_gt_cache`), `scripts/heldout_manifest.py` (stable sanitized ids `belege-<de|en>-<email|scan>-NNN` + sha256 + page counts + `.eml` PDF-attachment extraction → local `index.json`; **sanitized** tracked datasheet = counts + per-field presence + id↔sha256 freeze table, **no filenames/values**), and a Streamlit **Ground Truth Review** page (`app/views/heldout_review.py`, a bounded WRITE exception like ADR-039) for field-by-field verification against the page image. **Drafter = Cascade** (one-time cloud read of the private invoices for the DRAFT only, author-verified field-by-field — verification is the scientific anchor); **circularity guard:** any contestant's raw extraction (cloud H1 / local arms) stays a SEPARATE artifact, never feeds the GT. **Privacy:** `data/self-collected/**` git-ignored in full (belt-and-braces `.gitignore` rule); only the sanitized datasheet is tracked. 16 synthetic-fixture tests; no grading in this issue. Sub-decision of ADR-034; mirrors ADR-012/013 GT/scorer contract; closes #78. | Accepted |
| ADR-041 | Full-coverage invoice extraction schema (Step 1: flat fields + VAT breakdown + Skonto) — extends ADR-035's 19-field schema toward complete EN16931 / §14-UStG coverage so the reported F1 describes the whole extraction job, not a 19-field subset; adds **document type** (BT-3), the **payment block** (due date BT-9, means code/text BT-81/82, derived status, bank IBAN/BIC/holder BT-84/85/86, reference BT-83), **prepaid/allowance/charge/rounding** totals (BT-113/107/108/114), **order reference** (BT-13) and **billing period** (BT-73/74) as flat scored fields, plus the **per-rate VAT breakdown** (BG-23) + **Skonto tiers** as simple repeating groups (alignment by natural key); reworks the subtractive frozen-16 set into an explicit positive list so closed-milestone published numbers stay frozen as `FIELDS` grows; full field model in `docs/architecture/invoice-field-model.md`. Line-item table (BG-25) + its smart-matching scorer deferred to Step 2 / ADR-042. Extends ADR-035; sub-decision of ADR-034; substrate for #78 (held-out GT drafting) + #104 (eval audit). | Accepted |
| ADR-042 | Unified repeating-group scoring (VAT breakdown · Skonto · line items) — Step 2 — broadens the reserved line-item-only scope into ONE uniform scorer for all three list-shaped parts of the schema. **Greedy maximum-similarity bipartite row alignment** (similarity = fraction of the GT row's gradable cells the prediction reproduces; the DocILE LIR standard, arXiv 2510.15727 / 2405.20245) pairs predicted↔GT rows; matched rows score every cell via the flat truth table (refactored `_score_one_field` → spec-explicit `_score_against_spec`), missed GT rows score FN (hurt recall), spurious predicted rows score FP (hurt precision). New `score_repeating_group` + `RepeatingGroupResult`; `score(predicted_groups=...)` folds all cells into a new headline `overall_micro_f1` (flat + repeating) while the flat `micro_f1` is retained unchanged. **Opt-in** so cohort runs + frozen in-sample baselines are provably unmoved (CII parsing now always populates the groups). Line-level v1 xpath substitutions added. Sub-decision of ADR-041; extends ADR-013/ADR-027. | Accepted |
| ADR-043 | Ground-truth validity: optional-zero EN16931 totals (BT-107/108/113/114) treated as absent for visual-extraction scoring | Accepted |
| ADR-044 | JSON-recovery structural-repair rung — a string-safe, value-preserving final rung in the `adapters_json` recovery ladder that supplies delimiters the structuring model omitted (a value object's missing `}` before an array's `]`; bracket/brace truncation), so a single malformed brace can no longer zero an otherwise-complete invoice (the Arm-B `EN16931_Rabatte` micro_f1=0.000 measurement artifact). Conservative: declines mid-string truncation (honest all-null contract preserved per ADR-018/038); never invents keys/values (honesty guardrail per ADR-034). Extends ADR-018/038. | Accepted |
| ADR-045 | Ground-truth validity: flat `tax_rate` (BT-119) EXCLUDED for multi-distinct-rate invoices — the document-level flat `tax_rate` is well-defined only for single-rate invoices (ADR-035's stated intent); when the CII carries multiple distinct `ApplicableTradeTax/RateApplicablePercent` values (e.g. 7%+19%) there is no single "the rate" on the page (the per-rate truth lives in the `vat_breakdown` group), so scoring a flat scalar is ill-posed. `parse_cii_xml` marks it EXCLUDED (neutral; reuses the scorer's `is_present=True`+`normalized_value=None` path) for >1 distinct rate; single-rate invoices score unchanged. Sibling ruler fix to ADR-043; extends ADR-035. | Accepted |
| ADR-046 | Ground-truth validity: `document_type` (BT-3) code map covers the full EN16931/UNTDID-1001 invoice family — the page renders the German word ("Rechnung") or a generic invoice layout, never the numeric subtype code, so a model that reads "Rechnung"→"invoice" is correct regardless of which invoice-family code the issuer chose. `_DOCTYPE_CODE_TO_TOKEN` extended from {380,389,381,384} to the full family (380/386/387/388/389/393/395→invoice; 381/396→credit_note; 384→correction), fixing the Miete `387` (hire invoice) false-negative. Sibling ruler fix to ADR-043/045; extends ADR-035/041. | Accepted |
| ADR-048 | Scoring fairness: predicted-side `category_code` (BT-118) normalizer — the EN16931 VAT-category letter (S/Z/E/AE/K/G/O) is a controlled-vocabulary code that is NEVER printed on the page (the page renders the German word "Umsatzsteuer" / "steuerfrei" / "innergemeinschaftliche Lieferung"), so scoring the model's rendering against the raw code letter manufactured a systematic FN on every invoice's VAT-breakdown row (capping each group's F1 even when rate/amounts were perfect). Adds an optional `FieldSpec.predicted_normalize` hook (overrides the scorer's `field_type` dispatch; open/closed registry extension) and a `_normalize_predicted_vat_category` normalizer that recovers the code from the model's output — bare code, parenthesized code ("Umsatzsteuer (S)"→S), or German/English phrase (specific-first synonym scan; ß/ss-spelling-robust). Representation-only: a model naming the WRONG category still maps to the wrong code and scores FN (never masks errors). Rejected prompt-guidance alternative (measured: it fixed category_code but perturbed model behavior, flat spurious 0.071→0.357) in favor of an isolated, deterministic ruler fix. Sibling to ADR-043/045/046/047; extends ADR-013/027/041/042. | Accepted |
| ADR-047 | Ground-truth completeness: free-text payment-terms fallback (net due date BT-9 + Skonto group) — the synthetic ZUGFeRD corpus (and many real invoices) carry the net due date + Skonto in the free-text `SpecifiedTradePaymentTerms/Description`, not the structured `DueDateDateTime`/`ApplicableTradePaymentDiscountTerms`; for a visual-extraction task the page renders these, so the GT must too, else a model that correctly reads them is scored FP (precision artifact) and one that misses them is rewarded. `parse_cii_xml` parses conservative German patterns (`netto bis DD.MM.YYYY` → BT-9; `X% Skonto innerhalb N Tagen` → skonto tiers, comma-decimals canonicalized) ONLY when the structured field is absent (structured always wins; non-match → absent, never fabricated GT). Implements the "structured-text fallback" the Skonto group flagged as a follow-up. Sibling ruler fix to ADR-043/045/046; extends ADR-041/042. | Accepted |
| ADR-049 | Registry-driven structurer field glossary (totals + buyer references) — fixes systematic *comprehension* errors where the structuring model (Gemma) misassigned the 5 document totals (`line_total_amount` BT-106 / `tax_basis_total_amount` BT-109 / `tax_total_amount` BT-110 / `grand_total_amount` BT-112 / `due_payable_amount` BT-115) to a single line / single-VAT-rate subtotal, and swapped `buyer_reference` (BT-46 Kundennummer) ↔ `buyer_order_reference` (BT-13 Bestellnummer), because the prompt carried only a bare English key list with no anchor to the German labels printed on the page. Adds two optional `FieldSpec` attributes — `description` (generic field semantics) + `prompt_aliases` (example German label names) — populated for the 7 confusable fields (NEVER ground-truth values, so the guide is identical for every invoice + locale). A `{field_glossary}` placeholder in the structuring prompt is filled from the registry at ONE substitution point (`structurer.render_field_glossary` / `render_structuring_prompt`), wired into Arm A (harness), Arm B (`run_arm_b`), and the live demo so all paths render the guide identically. Generic + declarative + no GT leakage + no invoice-specific instructions. Measured before/after on the 6-invoice dev cohort: flat micro_f1 +0.028..+0.164 on 5/6 invoices, unchanged on 1, **no regression on any**; `spurious_emission` unchanged on 5/6 + improved on 1 (Gutschrift 0.071→0.000) — confirming it avoids the honesty regression that killed the rejected ad-hoc prompt-guidance attempt (ADR-048: flat spurious 0.071→0.357). A PROMPT fix (distinct from the ADR-043..048 scorer/GT ruler-fix lineage); extends ADR-035/038/041/042. | Accepted |
| ADR-050 | Scoring fairness: predicted-side `invoice_number` (BT-1) label stripping — the structuring model faithfully transcribes the invoice number together with its printed LABEL (`"Nr. 471102"`, `"Rechnung Nr. 471102"`), but the GT `ram:ID` is the bare identifier (`"471102"`), so a literal-CODE comparison manufactured an FN on 5/6 dev invoices over a label the model copied correctly (the same as-printed-vs-as-stored class as ADR-046/048). Adds `normalizers._normalize_predicted_invoice_number` (wired via the ADR-048 `FieldSpec.predicted_normalize` hook on the `FIELDS["invoice_number"]` entry) that strips a leading number-label (`Nr.`/`No.`/`Rechnung[s]nr.`/`…nummer`/`Invoice No.`) ONLY when a separator run (`[.:#]+` and/or whitespace) follows — so a genuine identifier that merely starts with those letters (`NR-2024-001`, `INV-001`, `NO2024`) is never eaten — then applies the standard predicted-CODE normalization. Representation-only: a wrong number still scores FN; a value with no label (Miete's `9314110911/00/M/00/N`) is unchanged; a bare-label echo (`"Nr."`) falls back un-stripped (honest FN, never empty). Offline rescore of the saved Arm-B transcripts (no re-inference): flat micro_f1 +0.022..+0.030 on 5/6 invoices (Miete already TP), no regression, `spurious_emission` unchanged (a scorer-side fix touches no model output). Sibling to ADR-046/048; extends ADR-013/027/041. | Accepted |
| ADR-051 | Scoring fairness: predicted-side optional-zero totals normalizer (BT-107/108/113/114) + `seller_assigned_id` (BT-155) GTIN/EAN-suffix stripping — two predicted-side ruler fixes symmetric with the GT-side optional-zero rule (ADR-043). (1) The structuring model sometimes emits a structural `0.00` for an absent optional total (allowance/charge/prepaid/rounding); since GT treats those as ABSENT (ADR-043), a literal-MONEY comparison manufactured an FP — `_normalize_predicted_optional_zero_money` maps a predicted `0`/`0.00` on these four fields → None (absent), so the predicted side matches the GT convention. (2) The model often appends the line GTIN/EAN with a marker (`"PFA5 4000001234578 (GTIN)"`); the GT `SellerAssignedID` is the bare article id, so `_normalize_predicted_seller_assigned_id` strips a trailing marked GTIN/EAN → bare id. Both wired via the ADR-048 `FieldSpec.predicted_normalize` hook; representation-only (a genuinely wrong value still scores FN/FP — never masks an error). Sibling to ADR-046/048/050; extends ADR-013/027/041/043. | Accepted |
| ADR-052 | Ground-truth validity: flat `tax_rate` (BT-119) EXCLUDED for single **0%-rate** invoices (reverse-charge / intra-community / exempt) — extends ADR-045 (which excluded the flat scalar for multi-distinct-rate invoices) to the single-zero-rate case. When the only VAT rate is 0, no positive "tax rate" is rendered on the page (it shows "0 %" / "steuerfrei" / "innergemeinschaftliche Lieferung" / "Reverse Charge") and the literal 0 is already captured per-rate in the `vat_breakdown` group, so scoring a flat scalar 0 rewards/penalizes a redundant, ill-posed field. `parse_cii_xml` marks it EXCLUDED (neutral `is_present=True`+`normalized_value=None`, same path as the multi-rate exclusion); a positive single rate (e.g. 19) is unaffected and still scored. Sibling ruler fix to ADR-043/045/046/047; extends ADR-035/045. | Accepted |
| ADR-053 | Structurer field glossary stays flat-only — repeating-group (line-item) cell extension measured net-negative and rejected. ADR-049's `render_field_glossary` is open/closed (any `FieldSpec` with a `description` auto-extends the guide), which invites glossing the confusable line-item cells (`name` vs `seller_assigned_id` vs `net_price`) to lift Innergem's sub-0.90 *overall* (flat+groups) F1. A clean A/B on the 6-invoice dev cohort (greedy decoding ⇒ deterministic) measured the extension: **flat `micro_f1` unmoved on all 6** (the headline metric — line-item cells are not in it), and **overall** gained only +0.016 on Innergem (0.861→0.877, still <0.90) while **regressing** Rabatte (0.920→0.901) and XRECHNUNG (0.990→0.968) — net −0.026 across the cohort, the same greedy-decode prompt-brittleness that killed an ad-hoc `net_price` refinement in the same session. Decision: the glossary anchors flat scalars only (ADR-049); repeating-group cells are deliberately NOT glossed. The residual Innergem line-item misses (name/article-number merge; reader IBAN `DE`→`DF` slip) are genuine model/reader-ceiling errors, not generically fixable without invoice-specific guidance. Sibling to ADR-049; documents a measured rejection (cf. ADR-048). | Accepted |
| ADR-054 | Thesis endgame: reader-first recovery + scope freeze — the attribution audit (`eval/finetune-attribution-audit.md`) proved the 0.6771 sealed-val zero-shot baseline is **reader-dominated**: the unmodified structurer scores 0.9608 on perfect (oracle) transcripts (line items 0.971, VAT 0.986), so eval definitions are NOT too hard (~0 share) and ~0.28 of the 0.32 gap is reading-induced vs ~0.04 structurer capability. Decision bundle: (1) the GPU reader bake-off (#114) runs with `opendatalab/MinerU2.5-Pro-2605-1.2B` as lead candidate (MinerU 3.3-release checkpoint; fixes 2604 issues + native multilingual OCR; OmniDocBench v1.6 lineage = 95.69 SOTA) vs `Qwen3-VL-4B` (architectural contrast) vs granite-docling-258M (control); answerability decides, ties break smaller. (2) Transcripts regenerated with the winner → re-baseline → **conditional LoRA**: fine-tune the structurer ONLY if the re-baseline stays < 0.90. (3) **Scope freeze**: Layers 2–3 (graph RAG, H3–H6), the cloud arm (H1 full form), and template-shift (H7) move to the thesis future-work chapter; after the reader-recovery milestone closes, the experiment track FREEZES and remaining capacity goes to the writeup phase. Extends ADR-007/009 (cohort), ADR-031 (hypothesis honesty), #55/#114. | Accepted |
| ADR-055 | Thesis manuscript authoring setup — in-repo `thesis/` (LaTeX; FH Wedel KOMA/biblatex template adapted to **English**), built with `latexmk` (pdflatex + biber) via `make thesis` → `thesis/_build/main.pdf`. Decides location (in-repo vs separate repo), format (LaTeX vs Quarto), language (English; German `Kurzfassung` optional), build tooling, and programmatic results integration (figures/tables generated into `thesis/figures` + `thesis/tables`, never hand-copied). New top-level `thesis/` dir (invokes the `clean-project-structure` ADR-exception). **Authored as 054 on a parallel branch; renumbered to 055 after ADR-054 (endgame) landed first — see the numbering note in the record.** **Amended 2026-07-31** to align with ADR-054's scope freeze: chapter map reshaped (three-layer vision → a *System Design* chapter; unbuilt KG/query layers → a substantial *Limitations and Future Work* chapter; no empty chapters), front/back-matter order corrected to the Richtlinie (abstract now follows the abbreviations list, not the ToC), statutory declaration replaced with the verbatim required wording (AI clause inline), and title-page personal-data policy fixed (name + Matrikelnummer + examiners committed; no postal address in the manuscript). | Accepted |
| ADR-056 | Answerability-ruler honesty fix + blank-page reader guard (#114 bake-off investigation) — text-layer probe exposed a 0.794 measurement ceiling; representation-only variant expansion (composite addresses component-wise, document-type surface words, currency symbol, compact IBAN, slash dates) lifts the perfect-text ceiling to 0.941; `skip_blank_pages` guard in the reader pass closes the blank-page-hallucination failure mode (Qwen3-VL invented a fictional invoice on a blank page: 0.885 → 0.047 overall F1) | Accepted |
| ADR-057 | Reader selection — Qwen3-VL family (4B winner by corrected findability 0.970 + mechanism audit; olmOCR-2-7B rejected for silent margin-block drops despite +0.022 F1; one pre-registered 8B sibling test during the regen relaunch; findability protocol becomes standing methodology; new canonical transcript lineage supersedes granite per ADR-011). **8B sub-decision resolved 2026-08-07 — 4B stands confirmed.** Adjudicated from the already-committed 2026-08-04 bake-off artifacts with no new GPU spend (the run had happened; nobody had judged it). The pre-registered rule is conjunctive and the 8B **fails clause (b)**: it decode-collapses on `zugferd_2p0_EXTENDED_Fremdwaehrung` (911 `&nbsp;` in a 5,466-char repeat run, `Belegsummen` truncated, BT-112/113/115 lost) — the MinerU-2605 loop-collapse class, which the 4B exhibits on 0/29. It wins clause (a) (13 < 16 misses, findability 0.976 vs 0.970) and clause (c) (1.247× ≤ 2.5× wall-clock), but a better headline number does not buy a worse mechanism — the same reasoning that rejected olmOCR at +0.022 F1. Consequence: canonical transcript lineage unchanged, no regeneration, no re-baseline, fine-tune gate stays 0.8257 < 0.90. Closes #114. | Accepted (resolved) |
| ADR-058 | Structurer prompt-surface + scoring-fairness correction (pre-LoRA) — an evidence audit of the three oracle-zero fields found **three independent defects, one per field family**: (a) BT-119 `tax_rate` was emitted only inside `vat_breakdown[].rate_percent` and left null in the flat key → deterministic single-rate backfill (repair, not invention); (b) BT-113 `prepaid_amount` was emitted signed as printed (`-50.00` vs GT `50.00`) → sign fold, applied to **both** sides after the corpus was found sign-inconsistent (BT-107 = `-0.23` on a credit note, so a predicted-only fold inverted a correct answer); (c) BT-107/108 were *invisible to the prompt* — `check_oracle_transcript_labels` proved all 6 allowance cases had `Summe Nachlässe: <value>` in the input while the model emitted `null`, i.e. a glossary gap, **not** a scorer bug, and explicitly **not** a LoRA target (ADR-048). The audit additionally found **4 ground-truth values leaked into glossary descriptions** (a VAT id, a slashed Steuernummer, a payment-method phrase, an example date) and **34 `prompt_alias` entries with 0/146 corpus occurrences** (incl. `'Summe Nachlässe'` itself, while the corpus prints `'Gesamtbetrag der Abschläge'` 88/146, and a missing `'USt.-Id.-Nr'` printed 91/146). Prompt surface is now 100% corpus-grounded and leak-free, gated by `make audit-prompts` + a hermetic structural test. Zero-shot baselines superseded; re-generation required before the LoRA gate. Extends ADR-043/045/046/048/049/051/052/053/056/057. **Accepted 2026-08-06** once the Tier-2 re-generation it conditioned itself on was recorded: BT-107/108 moved off zero on both arms (reader 0.000→0.667 / 0.889→1.000; perfect 0.000→0.909 / 0.000→1.000). Three fields moved *down* and are documented as non-regressions — `payment_means_text` 0.286→0.133 on reader text is the **leak removal working** (the pre-fix number was inflated by a leaked payment-method phrase; perfect text stays 1.000, proving capability), and `line_total_amount` / `delivery_date` perfect-text dips are ADR-059 honest-ceiling corrections. Its "known limitation" (22/34 `german_label` values absent from the corpus) is **resolved by ADR-059**; its `docs/sources/standards/` citation was corrected — that directory never existed. | Accepted |
| ADR-059 | Oracle-transcript fidelity + group-level reporting — the perfect-transcript arm is a *measuring instrument*, so a wording it prints that no invoice prints makes the ceiling **wrong in an unpredictable direction**, not merely optimistic (`charge_total_amount` scored 0.000 on the perfect page while scoring 0.889 on real reader text, purely because the rendered label was invented). Three coupled fixes: (a) `FieldSpec.printed_label` + `rendered_label` become corpus-grounded assertions **gated** by `make audit-prompts` check B, with a documented no-printed-label exception list; (b) repeating-group cells render as `<label>: <value>` (was `<label> <value>`, which silently cost **103 cells on PERFECT input** once labels got short) with a one-line-per-row invariant and GT-position ordinals rather than a counter; (c) `compare_eval_reports.py` diffs the **group** surfaces alongside the flat ones, and `finetune/evaluate.py` reports per-cell outcomes, so a pooled-headline move can never again have no visible cause. Guarded by 6 hermetic tests in `tests/test_finetune_dataset.py` (corpus is gitignored, so corpus-gated tests would never run in CI — and this is precisely the invariant that broke there). **Authored retroactively 2026-08-06**: the work shipped citing "ADR-059" from 4 modules while the record itself was never written; the number had been reserved in this index to stop reuse. | Accepted |
| ADR-060 | Held-out ground-truth authority — cloud vision judge (Anthropic Messages API, Opus-class, `output_config.effort="xhigh"` + schema-locked structured outputs) reads the 300 DPI page images to author held-out GT, superseding the text-layer-drafted `drafted_by: cascade` GT that was proven unusable (hallucinated values on the 10 image-only scans; 19/37 flat fields and 0/3 repeating groups covered, which mechanically zeroed `line_items`/`vat_breakdown`/`document_type`) | Accepted |
| ADR-061 | Second held-out GT channel — Azure AI Document Intelligence `prebuilt-invoice` (F0, EU region), read per-page as PNG rasters, chosen because a specialist OCR + invoice field model fails differently from a generalist VLM (agreement between two systems that fail the same way is worth little). Declares `azure-ai-documentintelligence` as a direct dependency, runs over all 39 documents rather than Tier B only (quota + auto-accept reasoning inside), records Azure silence as `not-covered` distinctly from `not-present`, and treats per-field `confidence` as triage ordering only — never as a warrant. GT-authoring only; never on the inference path (ADR-057 unaffected). | Accepted |
| ADR-062 | Held-out GT provenance schema (`schema_version: 2`) + adjudication model — every promoted held-out cell carries one of five provenance classes (`text-layer-proven` / `two-channel-agreed` / `author-adjudicated` / `exempt-by-policy` / `null-claim`), stored inside the GT document rather than in a sidecar so a warrant cannot desync from the value it warrants. Defines the auto-accept rule (printed-evidence `proven` **and** two independent channels agreeing), the escalation ranking, and the coverage-vs-conflict separation that keeps a judge-only cell from being counted as a disagreement. Invisible to `build_groundtruth_from_json`, so published ZUGFeRD figures cannot move. Extends ADR-040/058/060/061. | Accepted |
| ADR-063 | Held-out grading scope + answer-key source — a held-out run reads the author-signed-off `_promoted/` key (never the `gt/` draft, which is an adjudication input channel) and reports the 34 flat fields only (**corrected 2026-08-06** from a stale "19", which predated two schema extensions; `len(FIELDS) == 34` was always pinned by `test_fields_registry_consistency`, so no measured number moved). Repeating groups are excluded because their rows were never author-reviewed; the exclusion is structural (`predicted_groups=None`, so group cells are not scored as spurious either) rather than a reporting convention. Line-item extraction continues to be measured on the synthetic ZUGFeRD corpus, where GT comes from the embedded factur-x XML and is exact by construction. Records the re-scored held-out figure (0.8767 flat micro-F1, 39/39 invoices, zero-shot). Extends ADR-040/062; scopes what ADR-034's held-out measurement claims. | Accepted |
| ADR-064 | **A prompt-fixable gap is never a fine-tune target** — states, as its own record, the attribution principle that five call sites had been sourcing to ADR-048. **ADR-048 does not contain it**: that record is a scoring-fairness fix for BT-118, and its Alternatives section says the opposite of the attributed claim — prompt guidance is "rejected as the *ruler* fix; it remains a legitimate, separately-measured model-improvement experiment." The principle itself is sound and load-bearing (a fine-tune trained over a prompt-shaped gap is credited with gains the prompt could have produced for free, and `finetune/dataset.groundtruth_to_target` builds LoRA labels from the same registry the prompt renders), so it is stated here with its real evidence — ADR-048's measured prompt-perturbation blast radius (flat spurious-emission 0.071 → 0.357) plus ADR-058's BT-107/108 glossary gap — and the four citing modules are re-pointed. Defines the ordering rule: repair the prompt, re-measure on the synthetic corpus, and only then evaluate the fine-tune gate. | Accepted |
| ADR-065 | Ground-truth validity: a held-out answer **no adjudication channel could locate in the page text** is scored EXCLUDED (neutral) rather than counted as a miss — for fields the registry flags, on the `null-disputed` warrant only. Extends the ADR-043/045/046/052 ill-posed-cell lineage to a *provenance* criterion, reusing the same neutral encoding (`is_present=True` + `normalized_value=None`). Applied to exactly one field, `payment_means_text` (BT-82), on evidence: it is the only scoring field whose every present held-out cell is author-adjudicated (**0** with printed-text proof, **0** with two-channel agreement), EN16931 derives a payment method from no other field, and the model recovers **0 of 8** such cells while scoring **1.000** on perfect transcripts — so the loss is page ambiguity, not comprehension. Deliberately **not** generalised: the criterion is confounded across fields (`due_payable_amount` is unlocatable on 12 cells yet recovered on 11, because it is fixed arithmetically as gross − prepaid + rounding), and keying on the `class` field instead of `escalated_as` would be wrong because `author-adjudicated` lumps genuinely-absent cells together with merely-non-verbatim ones (`document_type` scores 36/39 in that same class). Wired as `FieldSpec.neutralize_when_unlocatable` (open/closed, mirroring `predicted_normalize` / `printed_label`); a missing or malformed warrant fails safe to *scored*. Round-1 effect, re-scored offline from frozen generations so it is attributable to the ruler alone: field F1 0.462 → **0.667**, pooled cell F1 0.8931 → **0.8987**, recall 0.8402 → **0.8503**, FN 108 → **100**, with **TP and FP unchanged** (568/28) — not one correct answer discarded. Provenance-free callers are byte-identical, so ZUGFeRD figures cannot move (ADR-062 invariant preserved). 12 hermetic tests. | Accepted |
| ADR-066 | Prompt-gap classification, applied — replaces a proposed 34-field alias-adding walk with `scripts/classify_field_gaps.py`, which compares the reader arm against the perfect-text (oracle) arm on the two committed val reports and requires **two or more** errors on perfect text before a field escalates (one error on a small denominator, e.g. `billing_period_start` n=3, is recorded `marginal`, not escalated). Verdict over 34 flat fields: 1 label-mapping, 2 prompt-candidate, 8 marginal, 18 reading-gap (proven hands-off), 4 closed, 1 untested. Per-invoice escalation of the 3 candidates found **zero** are prompt-fixable: `line_total_amount` is a net-vs-gross confusion between two *already-grounded* aliases that lands mostly on the **oracle** arm (which is why oracle 0.863 < reader 0.906 — no headroom exists); `seller_iban` emits a character-identical value on 11 of its 12 carriers, with 4 of 5 FNs being the pre-existing IBAN normalizer asymmetry (`finetune/dataset.py`), one a reader-context miss, plus 3 hallucinated FPs where no IBAN exists; `seller_name` is corrupted ground truth on 2 of 3 (embedded newline, GLN pollution), the third a company-vs-contact-person error on one French credit note (sub-threshold). A pre-merge self-audit re-derived every claim from the committed artifacts and corrected four evidence errors in the first draft (§"Self-audit correction") without changing the outcome; the 34-row table was machine-diffed against the JSON and was clean. Zero registry/preamble edits made; zero re-measurement needed (the committed reports already describe the unchanged prompt, confirmed by an exact `--score-only` reproduction). Adds a per-field FN-readability split to `finetune_attribution.py` that closes a blind spot in the oracle-ceiling inference itself (the oracle page prints the registry's own label, not the reader's wording), and records the real-invoice vocabulary-narrowness limitation with numbers: `delivery_date` scores 0.865 on the grounding corpus (ceiling 0.974) vs 0.435 on the 39 held-out real invoices, and `Lieferdatum` is 0/146 in the grounding corpus. Extends ADR-064 (the ordering rule this discharges) and ADR-058/059 (the arms and prompt surface being classified). | Accepted |
| ADR-067 | Structurer LoRA — recipe, **2×2 attribution design**, and pre-registration (#55). ADR-054's conditional gate is met (re-baseline 0.8257 < 0.90) and ADR-064's ordering rule is discharged (ADR-066 found zero prompt repairs), so the LoRA is warranted. But the committed reports show **Gemma already scores 0.9719 on perfect GT-rendered text** — above the 0.90 gate — so the shortfall is *reader-noise robustness*, not raw capability, and a reader-arm gain measured alone would be uninterpretable (schema-learning vs noise-robustness are different claims). Surfaced by a user challenge that rested on an "oracle"-is-a-model conflation but exposed a real framing error. Decision: train **two** adapters differing only in input distribution (reader transcripts vs `render_oracle_transcript` output, reusing ADR-059's renderer via a new `reader_text_fn` seam mirroring the one `evaluate_structurer` already had), then evaluate each on both conditions. The oracle adapter is an **instrument, never a product**. Checkpoint selection moves to a dev slice carved from TRAIN (`split.carve_dev`, 100 fit / 17 dev, zero overlap with the sealed 29, a derivation that never rewrites `split.json`) — previously the trainer passed the sealed 29 as `val_dataset` with `val_batches=-1`, computing loss on the reported set every run. Selection delegated to HF (`load_best_model_at_end` + `metric_for_best_model=eval_loss`) rather than hand-rolled. Cosine LR + 3% warmup replaces a constant rate; 6 epochs is a budget, not a target. Binding pre-registration: min dev loss selects, sealed val scored once per arm, matched-stack comparison only, **null result is publishable**, no sweep. Records as a thesis limitation that the structurer was never competitively selected (contrast ADR-057's 5-candidate reader bake-off) — the honest answer to "why Gemma?". | Accepted |
| ADR-068 | Fine-tune venue — **CUDA + TRL/PEFT instead of local MLX**, with a mandatory matched-stack baseline re-measurement (#55). Evidence: a 4-example / 4-iteration path-validation smoke ran long enough to be cancelled on an M1 Pro 16 GB, against a real budget of ~600 forward passes; `train.py` already carries three Metal-ceiling workarounds (freed vision/audio towers, a manual `grad_checkpoint` patch because mlx_vlm's own check silently no-ops on gemma-4's nested layers, a held wired-limit raise), i.e. the local path was at its edge before this run. Plus `know-your-hardware`: it is the user's only machine. Chose TRL `SFTTrainer` + PEFT over a hand-rolled loop because ADR-067's entire selection rule is native config — the venue change makes the discipline better-tested, not harder. **The trap avoided**: the committed 0.8257 baseline is MLX **4-bit**, a CUDA adapter is **bf16**, so the naive delta would measure the adapter *plus* a quantisation change, violating ADR-034/038 matched precision while looking like a clean before/after. Rejected 4-bit QLoRA (bitsandbytes 4-bit ≠ MLX 4-bit — would look matched without being matched) and adapter format conversion (untested correctness risk under the headline number). Also adds `CudaStructurerExtractor` + an injectable `extractor` on `evaluate_structurer`, without which the GPU box would produce adapters that cannot be scored anywhere. MLX path byte-for-byte unchanged when nothing is injected, so no committed report moves. `train.py` retained per ADR-011. Partially supersedes ADR-007 for **training only** — delivered inference stays fully local. `trl`'s type information caught two real defects: `lora_alpha` int-vs-float (rounding would change the alpha/rank scaling) and `completion_only_loss` defaulting to `None`/auto (decides whether loss covers the ~3k-token prompt). | Accepted |
| ADR-069 | `docs/reviews/` subdirectory — canonical home for manuscript/artifact review records (first occupant: the 2026-08-15 first-supervisor thesis review) | Accepted |
| ADR-070 | Measurement venue scoped per stage under a **two-tier on-premises envelope** — third-pass review found the ADR-054/057 rented-GPU session produced **every** reported transcript (reader bake-off + 146-invoice regeneration + 39 held-out), so the "4-bit / Apple silicon" arm labels describe the *structurer* only and six manuscript sites still claimed "fully/entirely local" for the headline result. Same-day author amendment fixed the first correction's over-rotation: the rented venue was one A10G 24 GB (single consumer-card class, EU, single-tenant, author-controlled open weights, author-owned data — 58 pages in ~16 min, zero errors, <6 GPU-h total), so the deployment claim is re-anchored to a **target class** (24 GB-GPU workstation, where every reading figure + the whole adaptation study were measured) and a **floor** (16 GB laptop, structuring measured); the floor's reading quality is the one declared-unmeasured number, with a bounded closing measurement (#129). Supersedes the partial fix in the 2026-08-16 second-review Addendum 3. | Accepted |
| ADR-071 | Legal-claim precision in the manuscript's motivation — the professional-secrecy premise is sourced per profession (`§ 62a StBerG` / `§ 43e BRAO` / `§ 50a WPO`, all from BT-Drs. 18/11936) instead of to the tax-advisor statute alone; the data-protection sentence is corrected against the EDPB's own published position (an EEA processor can still be reached by third-country law; contract wording does not cure it; only technical measures can where FISA 702 applies); the provider-amendment claim is narrowed to what is provable (Microsoft publishes one, Google's is NDA-only, AWS is individual-agreement, direct model APIs have none); and the two strongest available supports are added (professional secrecy protects legal persons where data-protection law protects natural ones — decisive for a B2B-invoice thesis; and a data-protection processing agreement is not the confidentiality instrument). Apparatus in footnotes to preserve the ch.1 scope promise. Also records three bibliography fabrications found in the same pass. Closes the supervisor gate of #96. | Accepted |
| ADR-072 | Held-out exclusion causes separated and disclosed — the fifth-pass audit found that `normalized_value is None` was carrying **two** unrelated meanings into the scorer's single `normalizer_rejected` state: ADR-065's ratified neutralisation (8 cells, one field, declared in the manuscript) and a silent third class nobody chose (22 cells, all `DATE`, where `validate_and_repair` could not coerce the author's own signed-off value). Both encode as EXCLUDED, so both vanish from F1, precision and recall alike, and the published 668-cell denominator is already net of all 30 — meaning no aggregate could ever have revealed the second class. Concentrated 17/22 in English email invoices, i.e. in the channel the system reads *best*, so the omission does not flatter the headline. Decision: disclose the class in the measurement-validity chapter's own defect chronology rather than repair it under the submission deadline (repair moves a published figure and needs its own regression pass — #118 stays open), and add `scripts/audit_heldout_exclusions.py` + `make audit-heldout-exclusions` so the count is re-derivable by command instead of quoted from a chat log. Read-only; classifies via the scorer's own `_gt_state` so the report cannot drift from the scorer. Also corrects two manuscript sites that misattributed the ADR-059 renderer defect to omission ("the renderer never rendered them") when ADR-059's own finding is mislabelling — the renderer emitted the standard's schema vocabulary, which no page in the corpus prints. | Accepted |
| ADR-073 | Statutory declaration corrected against the prescribed source — the fifth-pass audit's one true submission gate (U1) was that `thesis/preamble/declaration.tex` had never been verified against Richtlinie 3.0, and verification found a real defect: the second sentence read "in ähnlicher Form" where the Richtlinie prescribes "in gleicher **oder** ähnlicher Form", so the declaration declined to assure that the work had not been submitted in *identical* form. Corrected verbatim against "Abb. 2: Eidesstattliche Erklärung" (Richtlinie 3.0, Stand 25.04.2024, §3.9, p. 14). The same extraction cleared the two questions bundled with the gate — a German Kurzfassung is **not** obligatory (Tab. 1, p. 5, lists *Excecutive Summary* as `fallweise`) and the 80–120-Textseiten window (§3.5 item 4) is met at 113 body pages — and cleared the formal apparatus wholesale by checking it against FH Wedel's own LaTeX template rather than the Richtlinie's prose: margins, 12pt, `onehalfspace`, title-page geometry and Tab. 1 part order all match. Records that the prose margin figures (3,5/4/3/3 cm) describe the Word template and that "fixing" the LaTeX geometry to match them would have been a regression. | Accepted |
| ADR-074 | Citation style reversed to numeric brackets on the first examiner's written instruction, and the 2026-08-18 interim review registered — his annotated PDF carries a sticky note asking for "den IEEE oder ACM bibstyle … Nummern in Eckigen Klammern, was dann die Lesbarkeit verbessert". That falsifies the rationale standing in `thesis/preamble/header.tex:206-211`, which justified `authoryear` as matching "the first examiner's established preference" — an *inference* drawn from two prior graded works under the same supervisor, contradicted here by primary evidence from the supervisor himself. The audit also found the original `authoryear` choice had never been ratified in an ADR at all, so this record closes a `horus-decision-discipline` gap as well as reversing the decision. Decision: switch `biblatex` to a numeric bracketed style, rewrite the falsified comment rather than merely flipping the option, and rephrase the 17 `\textcite` call sites that read as sentence subjects and degrade under numeric rendering. Also ratifies `docs/reviews/2026-08-18-first-supervisor-comment-registry.md` as the tracked, anonymised register of all 38 review annotations — every target pinned by `/QuadPoints` word-box match (highlights) or an empirically derived nearest-element rule (sticky notes) — placed per ADR-069 rather than beside the PDF, with the review PDF and covering email gitignored in full because they carry the examiner's name and signature. | Accepted |
| ADR-075 | Frozen test-set distribution for examiners — the private 39-invoice held-out set becomes deliverable without weakening ADR-040's privacy posture: `make frozen-testset-bundle` stages the git-ignored tree (minus the regenerable page cache), renames the source PDFs to their sanitized `belege-*` ids (original filenames carry third-party names), rewrites `index.json` to bundle-relative paths, packs `tar.xz`, and seals it with AES-256-GCM under an scrypt-derived key (`cryptography` added as a dev dependency); the blob is attached to a GitHub Release on the (public) repo, and the examiner restores with one command — `make get-frozen-testset` downloads, decrypts (password handed over out-of-band), extracts to `data/self-collected/`, and verifies every PDF's sha256 against the committed datasheet freeze table, proving the restored corpus is byte-identical to the one the thesis evaluated. Public-but-sealed hosting chosen over a private mirror repo (auth friction for the examiner) and offline handover (no auditability, link-rot); GCM's authentication makes wrong-password and corruption fail loudly. | Accepted |
| ADR-076 | Submission repository presentation — the public tree is curated for its examiner audience without weakening the thesis's formal AI-usage disclosure (appendix E is, verbatim, the declaration's required marking; it stays untouched, as do `docs/decisions/` and `docs/reviews/`, both cited by the appendices as the reproducibility + review apparatus). Removed from HEAD: internal working documents (`docs/prompts/`, `docs/handoffs/`, `docs/retros/`, `docs/structure.md`), the workflow configuration (`AGENTS.md`, `.devin/` — untracked via `.gitignore`, retained locally), machine-local plan paths, and internal milestone tokens in living docs and docstrings; both READMEs rewritten audience-first. HEAD-only by choice: history is not rewritten (same tradeoff as the earlier supervisor-name redaction pass) because the disclosure makes old history consistent, not contradictory — this is curation of presentation, not concealment of process. | Accepted |

## Conventions

- Filename: `ADR-NNN-<kebab-slug>.md` (zero-padded NNN)
- Status: `Proposed` | `Accepted` | `Superseded by ADR-MMM`
- Never delete an ADR — supersede instead
- Format: Status / Context / Decision / Alternatives considered / Consequences (see any recent ADR)

Append a row manually as each ADR is added.
