# Architecture Decision Records

| ID | Title | Status |
|---|---|---|
| ADR-001 | Tool-decision discipline (mandatory ADR sections for every tool/model/library/dataset/framework choice) | Accepted |
| ADR-002 | Source-archival convention (`docs/sources/` tree + Obsidian-clipper-compatible stubs) | Accepted |
| ADR-003 | Brand naming — HORUS (Hybrid OCR-free Reading & Understanding System) | Accepted |
| ADR-004 | Config library — Pydantic Settings + PyYAML (implements `horus-config-discipline` Bundle 1) | Accepted |
| ADR-005 | Synthetic ZUGFeRD invoice generator — dual-track: `factur-x` (Python) for generation + Mustang Project (Java) for cross-tool validation | Accepted |
| ADR-006 | Visual PDF renderer for synthetic ZUGFeRD invoices — fpdf2 (py-pdf ecosystem) | Accepted |
| ADR-007 | Local-VLM inference framework — dual-track: MLX-VLM (primary, MLX-ported §8.1 cohort) + HuggingFace Transformers + MPS (fallback, non-MLX-ported cohort) | Accepted |
| ADR-008 | Orchestrated-baseline document pipeline — dual-track: Docling (primary, IBM Research) + MinerU pipeline backend (cross-check, OpenDataLab) | Accepted |
| ADR-009 | Pilot-loop VLM cohort selection — 10 models in 3 categories (end-to-end / architecturally innovative / general multimodal); resolves the "cohort ADR #14" forward-pointer from ADR-007 and ADR-008 | Accepted (amended 2026-05-15 — Amendment 1: evidence-limitation reframing + XML-grounded factual corrections) |
| ADR-010 | ZUGFeRD XML-extraction script — ratifies `factur-x` (already-installed Python library) as canonical extraction engine + Mustang as opt-in cross-tool verification route; ships `scripts/extract_zugferd_xml.py` as load-bearing infrastructure for pilot #13's XML-grounded F1 evaluation harness | Accepted |
| ADR-011 | Experiment-tracker integration — MLflow (full package, self-hosted local SQLite + filesystem artifact root); extends `Tracker` Protocol with `start_run`/`end_run`/`log_dict`/`set_tag` for pilot #13's per-field heatmap + hardware-fingerprint logging; closes `horus-config-discipline` Bundle 4 via additive L3 promotion to the `python-ml-uv` template | Accepted |
| ADR-012 | CII XML → ground-truth field dict — parser substrate for pilot #13's per-field F1 evaluation; ships `src/horus/eval/ground_truth.py` with 16-field EN16931-anchored English-keyed dict (`GroundTruth(header=...)`, BT-* preserved as record attribute), three-route dict-equivalence proven across the FX corpus, normalizer dispatch via `FieldSpec` registry; pilot #13's PR(a) of the locked 3-PR split | Accepted |
| ADR-013 | VLM prediction scorer — VLM raw output → predicted dict → per-field F1 against `GroundTruth`; ships `src/horus/eval/{scorer,adapters,anls}.py` triple, additive `field_type` amendment to `FieldSpec`, `EvalConfig` Pydantic sub-model, two-layer adapter (per-model preprocessor + unified German-label extractor), per-field-type comparators (ANLS\* for STRING + exact-on-normalized for MONEY/DATE/CODE), 12-cell truth table, micro/macro F1 aggregation; pilot #13's PR(b) of the locked 3-PR split; documents page-1-only smoke baseline as honest limitation deferred to PR(c) (ADR-014) | Accepted |
| ADR-014 | Cohort harness + multi-page rasterizer (pypdfium2) — pilot #13's PR(c) of the locked 3-PR split; ships `src/horus/eval/{rasterize,harness}.py`, `pypdfium2`-backed multi-page rasterization replacing the page-1-only `sips` baseline, `(model × invoice) → MLflow nested-run` orchestrator with `mlflow.search_runs`-based resume-safety, factur-x XML extraction route for ALL ZUGFeRD fixtures (NOT sidecar route per ADR-012 Probe 5), per-field heatmap PNG via `mlflow.log_figure` on parent run, threshold-sensitivity ablation at τ ∈ {0.3, 0.5, 0.7}, first thesis-defense-quality F1 numbers on the full 26-invoice FeRD ZUGFeRD corpus × 7 working ADR-009 cohort models; lifts the page-1-only scope ADR-013 deferred here; closes issue #13 | Proposed |
| ADR-015 | MLflow UI Makefile target (`make mlflow-ui`) — wires `mlflow server --backend-store-uri sqlite:///mlflow.db --host 127.0.0.1 --port 8080` into the project Makefile; supersedes ADR-011 §"What this ADR does NOT decide" line 306 deferral; documents `mlflow ui ≡ mlflow server` empirical equivalence in MLflow 3.12.0; locks port 8080 default to avoid documented macOS AirPlay Receiver conflict at MLflow's port-5000 default; closes issue #45 | Accepted |
| ADR-016 | Fast dev config + adapter-iterate harness mode — multi-file YAML composition via pydantic-settings native `deep_merge` (extends `ExperimentConfig.from_yaml` to accept `str \| list[Path]`); side-by-side adapter A/B re-scoring against cached transcripts via `scripts/rescore.py` (renamed from `ablation_threshold.py`) with stability self-check (Google Rules of ML §24) + opt-in MLflow logging (canonical 2-runs pattern); `dev_only` schema field for HARKing-prevention tier separation; `make adapter-iterate` Makefile target for the ~5-15s fast dev loop; closes issue #51 | Accepted |
| ADR-017 | Timing + tokens/sec + GPU-memory instrumentation for the cohort harness — extends `ExtractionResult` with `generation_tokens`/`generation_tps`/`peak_memory_gb` fields populated from MLX-VLM `GenerationResult` (MLX backends) + post-generation token-id shape (Transformers-MPS); aggregates per-tuple `perf.*` MLflow metrics (Amendment 1: split into `decode_tps_mean` decode-only AA-canonical + `inference_tps_mean` end-to-end + `tags.perf.decode_tps_available` availability tag — corrects original formula's AA-methodology claim after web-fetch re-verification; chars/sec as tokenizer-agnostic sanity check; peak memory via MLX `mx.get_peak_memory()` OR Transformers-MPS pre/post `torch.mps.driver_allocated_memory()` snapshot per pytorch/pytorch#104188 limitation); parent-level MPS-recommended-max ceiling for `%_max` derivation; extends `scripts/inspect_pilot_13.py` with `_print_perf_table` (plain-text, no `rich` dep, two TPS columns per Amendment 1, sorted by mean wall-clock ascending, graceful degradation for pre-#52 parents + backward-compat read of legacy `perf.generation_tps_mean` as e2e_tps); `make inspect-pilot-13` Makefile target; **substrate** for hypothesis H4 (latency-efficiency comparison) — H4 itself filed separately; closes issue #52 | Accepted (Amendment 1 ratified 2026-05-20 — formula correction) |
| ADR-018 | Structured-output prompting probe — extends the canonical pilot-13 harness with two declarative `CohortConfig` fields (`prompt_template_override: dict[str, str] \| None` + `adapter_mode: Literal["regex", "json"]`) and a sibling `src/horus/eval/adapters_json.py` module (same `(preprocess, to_predicted_dict)` public surface as `adapters.py`); two YAML overlays (`pilot-13-structured-probe-uniform.yaml` + `pilot-13-structured-probe-native-json.yaml`) drive a 7-model × 1-invoice × 2-arm probe over `EN16931_Einfach`; pre-registered ≥3-of-7 schema-adherence threshold gates the conditional follow-on issue (#54 Experiment 2 fires) vs the fall-through path (#41 Layer 2 MONEY adapter + #55 LoRA fine-tuning ADR); overrides issue #53 body's "no harness changes / no ADR" tactical guards on scientific-correctness grounds (apples-to-apples comparison vs pilot-13 baseline + reviewer reproducibility via one `make` command + zero code debt because probe artifacts ARE the Experiment 2 substrate if probe succeeds); closes issue #53 | Accepted with amendments (post-audit DEFER verdict at `d01afd1` superseded by ADR-019 bug catalog + ADR-020 rescore methodology + ADR-021 verdict matrix amendments; new 2×2 verdict surface in `eval/probe-verdict-matrix.md`) |
| ADR-019 | Structured-output probe bug catalog — locks the empirical evidence catalog of every bug discovered during the ADR-018 probe post-verdict audit (Gemma-4 unfenced 2-dict JSON parse failure / GLM-OCR fence-bias asymmetry / Granite decoder-loop placeholder echo / pre-registered threshold schema-mimicry false-positive / multi-page JSON adapter test coverage gap / PaddleOCR-VL decoder-collapse / MinerU decoder-loop / PaliGemma2 base-VLM pre-registration inclusion error / `scripts/inspect_pilot_13.py` hardcoded-experiment-name limitation). Per-row mechanism cited from saved transcripts. Sign-off gate before any fix wave lands. | Accepted |
| ADR-020 | Probe rescore methodology — ratifies the offline-rescore-from-saved-transcripts policy + `rescore_of` MLflow tag convention for ADR-019 Phase 4 (and any future post-audit rescore). Treats saved transcripts as canonical evidence (HELM + EleutherAI lm-eval-harness `--log_samples` precedent); historical MLflow runs NEVER mutated; each rescore is a NEW parent run in the same experiment cross-linked via `rescore_of=<original_run_id>` tag. Documents the model-behavior-vs-extractor-bug classification rule: re-inference is required only when an extractor bug is demonstrated (Wave 3.3); model-behavior failures (Granite/PaddleOCR/MinerU decoder-loops on JSON OOD prompts) are diagnostic-only and do NOT require re-running. | Accepted |
| ADR-021 | Structured-output probe verdict matrix + threshold/denominator amendments — ratifies the 2 × 2 verdict surface (pre-registered ∧ amended F1≥0.1) × (N-of-7 PaliGemma counted ∧ N-of-6 PaliGemma flagged) introduced by `src/horus/eval/probe_verdict.py` (Wave 3.2). Amends ADR-018's single-verdict pre-registration with: (a) value-non-trivial gate `micro_F1≥0.1` to defend against schema-mimicry per ADR-019 §B4 (GLM-OCR Arm A passes pre-registered with 16 placeholder keys, F1=0); (b) N-of-6 denominator that flags PaliGemma2 as structurally non-participating per ADR-019 §B8 (HF model card: base VLM, knowable at probe-design time per ADR-009 §smoke). Honest disclosure: this is methodology-discovery (a new failure mode unanticipated in ADR-018 §"Predicted outcomes"), NOT goalpost-move (pre-registered cells preserved verbatim; amended cells reported alongside). Locks the canonical_keys gap (non-null-value count interpretation penalizes the most-honest extractor Gemma-4-E4B-it which emits null for genuinely-missing fields). | Accepted |
| ADR-022 | `scripts/` directory status — ratifies `scripts/` as a Python package (`__init__.py` already present since `8d1c44d`); resolves the silent contradiction between the existing `__init__.py` and three in-file comments stating *"scripts/ is not a package — load … via sys.path injection"* (`scripts/compute_probe_verdict.py:50`, `tests/test_rescore.py:41`, `tests/test_inspect_pilot_13.py:25` + module docstring line 14). Mechanical migration replaces `sys.path.insert(scripts/)` + bare `import rescore` with `from scripts import rescore` everywhere; standalone-invocable scripts (`python scripts/X.py` direct-path) that import siblings get a small `sys.path.insert(0, repo_root)` at module top, documented as a known Python invocation-discovery idiom; pytest gets `pythonpath = ["."]` in `[tool.pytest.ini_options]` so the repo root is on `sys.path` during test collection (replaces the now-removed per-file `sys.path` inserts). mypy resolves the imports natively; the 3 `import-not-found` errors of issue #62 collapse to zero. Unblocks the import-portion of #62. | Accepted |
| ADR-023 | GitHub Actions CI pipeline — first CI for HORUS; ratifies `.github/workflows/ci.yml` running `make lint` + `make typecheck` (cache-cleared per the `mypy` incremental-cache nondeterminism trap surfaced in the audit) + `make test` on push-to-main + every PR against main, on `ubuntu-latest`. Test-gating mechanism: `pytest.mark.skipif` decorators (`skip_if_no_corpus`, `skip_if_no_fixtures`, `requires_macos`) centralized in `tests/_corpus.py` — the canonical pytest-recommended pattern + scikit-learn precedent (`sklearn/utils/_testing.py`). Three-layer application: module-level `pytestmark` (5 test files), test-level decorators (15 tests in `test_harness.py`), parametrized-fixture auto-disappearance (corpus-empty parametrize → zero items collected). NO custom marker registration; NO `make test-ci` target; NO CLI-flag gymnastics — `make test` works identically in local and CI environments. Design evolution audit trail preserved in §"Design evolution" (Iteration 1 marker design → Iteration 2 first-CI-run amendments → Iteration 3 skipif-unification redesign per pytest official + user-driven research). Justifies the design over committing the corpus to git (size + license drift) and over CI runners with corpus mounted (cost + maintenance). Closes the audit-followups cluster's CI gap and removes the discipline-only enforcement of `make-sure-it-works` for landed PRs. Closes #64. | Accepted |
| ADR-024 | EDA visualization stack — adopts **Quarto** (Posit) as the rendering layer + **Plotly** (interactive) + **matplotlib**/**seaborn** (static editorial figures) for thesis-grade EDA artifacts. Solves the user-stated "HTML gets messy fast" concern (issue #46) by rendering jupytext `.py:percent` source-of-truth into ONE archived HTML book + PDF with cross-references, citations, page numbers, figure captions, and code-folding. Layered ON TOP of existing `notebook-discipline` (jupytext stays the source-of-truth; Quarto is an additional renderer alongside papermill, NOT a replacement). New deps: `plotly` + `seaborn` in `pyproject.toml` (matplotlib already transitive); Quarto CLI binary installed via `brew install quarto` on dev machine; CI does NOT render (rendering is dev-machine-only per ADR-023's lint+typecheck+test scope). New `make eda CFG=...` Makefile target chains jupytext-sync + Quarto execute + render. Hybrid aesthetic per Q5 of EDA plan: matplotlib/seaborn editorial-static figures (FT/NYT-influenced muted palette, baked-in annotations) for thesis-appendix; Plotly interactive figures (hover-to-see-PDF-name on heatmaps) in a separate "Interactive Explorer" section in HTML output (drops to static fallbacks in PDF). Substrate for issue #46 EDA on the 151-PDF ZUGFeRD corpus; pattern reusable for any future thesis-grade EDA artifact. | Accepted (extended by ADR-025: multi-dataset Book structure + Datasheets-for-Datasets template) |
| ADR-025 | EDA scope expansion — multi-dataset Quarto Book + Datasheets-for-Datasets template — extends ADR-024's single-dataset visualization stack into a thesis-grade Quarto Book covering all 7 datasets in `data/raw/` (ZUGFeRD German + fatura2 + funsd + inv-cdip-tobacco + parsee-ai English + cord-v2 Korean + omnidocbench multilingual). Adopts Gebru et al. 2018 *Datasheets for Datasets* (arxiv:1803.09010) as the per-chapter dataset-documentation template (motivation / composition / collection / preprocessing / uses / distribution / maintenance). Structural changes: one chapter per dataset (`experiments/0X-<slug>.py` jupytext source) + cross-corpus synthesis chapter (`experiments/08-cross-corpus.py`) + consolidated Datasheets appendix (`experiments/A1-datasheets.qmd`) + index/preface (`experiments/00-index.qmd`) + bibliography (`experiments/references.qmd`); `_quarto.yml` extended with `book:` section + chapter list; new `src/horus/eda/` Python package factors out shared helpers (corpus walking + figure styling + Datasheet renderer) from per-dataset loaders. Per-chapter `configs/eda-<slug>.yaml` follows `horus-config-discipline` (one Pydantic-validated YAML per chapter; HARKing safeguards pre-committed before EDA runs). Local-only rendering during iteration (`_book/index.html` opened via file://); GitHub Pages deployment deferred to a separate ADR + commit once the Book passes end-to-end review. Out-of-scope: scope decisions about which datasets to use for evaluation/training/fine-tuning (those happen in PRD/spec phase, not EDA), ZUGFeRDv1 parser extension (future ADR if/when needed), Belege chapter (private/PII; separate gitignored artifact later). Driven by user feedback that the original ZUGFeRD-only EDA scope missed the 6 other datasets sitting in `data/raw/`. | Accepted |
| ADR-026 | Cohort dashboard TUI — textual inline-app three-section live dashboard for `make cohort-smoke` and `run_cohort`: scrolling per-invoice top panel + pinned current-model middle bar + pinned sweep-wide bottom bar. Display-adapter pattern (`HorusDashboardAdapter` / `PlainDisplayAdapter` / `SilentDisplayAdapter`) keeps harness display-agnostic. `App.suspend()` around HF tqdm model loads. Pyfiglet HORUS banner at sweep start. Graceful plain-print fallback when no TTY. Amends ADR-017's "plain-text, no `rich` dep" deferral for `_print_perf_table` (dissolved by this PR declaring `rich` as an explicit dep). New `src/horus/cli/` package. | Accepted |
| ADR-027 | F1 evaluation-metric expansion — four additive metrics on top of the existing micro/macro F1 (`ADR-013` scorer), all derived offline from the per-field `outcome` data already logged (no VLM, no re-run): (1) **per-canonical-label F1** — pools TP/FP/FN per each of the 16 `FIELDS` keys across the cohort (surfaces which business terms are hard); (2) **presence-conditional F1** — F1 restricted to GT-present fields (recall-faithful, since HORUS's truth table structurally yields FP only on the absent-GT row); (3) **group-level F1** — KIEval (arXiv 2503.05488 §4.1) all-or-nothing group correctness over EN16931 business groups (seller / buyer / totals), excluding the document-level scalars as the non-group G′; (4) **spurious-emission rate** — FP/(FP+TN) hallucination rate on genuinely-absent fields. (2)+(4) decompose micro-F1 into its recall/precision axes and jointly resolve the `ADR-021` canonical_keys asymmetry that penalised Gemma-4 for honest nulls. Extends `src/horus/eval/scorer.py` (additive `InvoiceFieldScores` fields + pure metric functions + `FIELD_GROUPS` partition) + `scripts/inspect_pilot_13.py` (cohort-pooled print). Resolves DR-7; closes #74. | Accepted |
| ADR-028 | Section-scoped invoice-totals adapter fallback (Belegsummen) — recovers the 4 silently-missing MONEY fields (`line_total_amount` BT-106 / `tax_basis_total_amount` BT-109 / `tax_total_amount` BT-110 / `grand_total_amount` BT-112) that the regex adapter dropped because their EN16931 `german_label`s (`Summe Nettobeträge` / `Steuerlicher Bemessungsbetrag` / `Umsatzsteuer gesamt` / `Bruttobetrag`) never match the FeRD "Belegsummen" display labels models actually emit (`Positionssumme` / `Rechnungssumme ohne USt.` / `Steuerbetrag` / `Bruttosumme`). Adds a section-anchored, shape-tolerant Layer-2 fallback in `to_predicted_dict` (`src/horus/eval/adapters.py`) that locally normalizes the Belegsummen window across the 4 totals-emitting transcript shapes (Granite DocTags `<fcel>…<nl>` cells / MinerU flattened `label: value` / PaddleOCR label-then-value lines / Gemma markdown table); section-scoping resolves the "Steuerbetrag" VAT-table-header collision AND preserves the single-page guarantee (page-1 transcripts carry no Belegsummen). Additively extends `scripts/rescore.py` with the ADR-027 4-metric baseline-vs-candidate summary (reuses the public scorer helpers; no change to the A/B comparison itself — extends ADR-016). Supersedes the `0.4908` cohort-baseline note in ADR-014 §"Known limitation" / §"Empirical results". No-HARKing: the metric set (ADR-027) + this fix were pre-registered before any post-fix numbers existed. Closes #41 | Accepted |
| ADR-029 | Structured-output JSON baseline (HND-1) — the "clean JSON" extraction arm (`adapter_mode="json"`, ADR-018) re-run on a 6-invoice ZUGFeRD subset for the 3 cohort models the ADR-018 probe proved JSON-capable (Gemma-4 / olmOCR-2-7B / GLM-OCR); cites ADR-019/021 for the 4 structurally-incapable models (DocTags / decoder-collapse / base-VLM) rather than re-running them (hardware-prohibitive + scientifically redundant per `know-your-hardware` + ADR-020). Re-inference (not offline rescore) was required — JSON-mode transcripts existed for only 1 invoice. Reuses the pre-registered Arm B prompts (per-model native prefix + JSON suffix) verbatim. Reports the ADR-027 4-metric surface via the existing inspector (no report code written). **Key findings (HND-3 #76 inputs):** (1) honest-null vs hallucinate-on-absent is the headline JSON axis — Gemma-4 spurious-emission 0.000 (best F1 0.707, fastest) vs olmOCR 0.875 / GLM-OCR 0.500 at *identical* presence-conditional recall; (2) prompt-only JSON under-recovers the Belegsummen MONEY totals that ADR-028's regex fallback recovers (`line_total` 0.105 vs 0.681) — "switch to JSON" does not obviously beat "free-form + adapter"; (3) KIEval group-level F1 ≈ 0 (cohort 0.019). `dev_only=false` (reported in-sample/diagnostic baseline comparable to `pilot-13-full`); No-HARKing via pre-registered plan+config+metrics. New overlay `configs/json-baseline.yaml`; no production-code change. Closes #54 | Accepted |
| ADR-030 | Reading-ceiling + approach-comparison diagnostic (HND-3 gate reframe) — reframes #76 from "pick the JSON-vs-free-form winner now" to "diagnose both honestly + carry both forward (staged); final Layer-1 pick deferred to post-fine-tuning" (H2 exploratory→confirmatory, arXiv 2503.08124). Ships `scripts/reading_ceiling.py` (offline, no VLM) + `src/horus/eval/transcripts.py` (shared transcript loader lifted from `rescore.py`, which is refactored to import it) + `make reading-ceiling` + the generated `eval/reading-ceiling-and-approach-comparison.md`. Adds (A) per-model raw-reading **ceiling** (upper-bound on extractable: a surface form of the GT value present in the raw transcript text); (B) the **parser-loss vs read-miss** split (readable-but-dropped = adapter problem; absent-from-raw = model problem), MONEY totals called out; (C) the **same-tuple 4-metric** free-form (post-ADR-028) vs native-JSON comparison on the 3 JSON-capable models × 6 shared invoices, per-model + pooled. Defuses the ADR-028 stale-MLflow landmine by re-scoring free-form LIVE from transcripts (never the F1=0.000 `pilot-13-full` saved scores). Determinism cross-check reproduces ADR-029 `json-baseline-metrics.txt`. **Diagnostic, NOT a verdict**; in-sample until the held-out Belege split (#78). Archives Tam et al. 2024 (arXiv 2408.02442, "Let Me Speak Freely?") + IBM Granite-Docling (single-model, anti-cascade) per `horus-source-archival`. Files follow-ups #88 (Pydantic JSON) / #89 (best-reader fine-tune) / #90 (Qwen2.5-VL) / #91 (parser-agent). Closes #76 | Accepted |

## Conventions

- Filename: `ADR-NNN-<kebab-slug>.md` (zero-padded NNN)
- Status: `Proposed` | `Accepted` | `Superseded by ADR-MMM`
- Never delete an ADR — supersede instead
- Format: see `~/.windsurf/rules/document-as-you-go.md`

This index is regenerated by `/docs-refresh` when it lands. Until then, append manually as each ADR is added.
