"""Dashboard pages, registered by `app/Home.py` via `st.navigation`.

Each page is a self-contained Streamlit script loaded through `st.Page(...)`. New
surfaces drop in here as new files and a one-line registration in `Home.py` — the
modular growth path described in ADR-036.
"""
